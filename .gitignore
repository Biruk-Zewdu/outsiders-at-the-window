# Editable paper sources are kept off the repo by choice — only the built
# paper.pdf is tracked. The originals live outside the project (see README).
paper.md
paper.tex

# Generated by src/build_pdf.py — a build artifact, regenerable from paper.md
paper.build.md

# pandoc / xelatex intermediates
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk

# OS / editor cruft
.DS_Store
__pycache__/

# Gephi / scratch exports
*.gephi
~$*
